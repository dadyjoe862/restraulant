import { ValidationError } from 'yup';
export type JsonError = {
    values: unknown[];
    error: string;
    message: any;
};
export default function errToJSON(error: ValidationError, target?: {}): Record<string, JsonError[]>;
